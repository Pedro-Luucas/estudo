#include <iostream>
#include <vector>
#include <cmath>
using namespace std;

int main(){
    int n,m,k;
    cin >> n >> m >> k;
    int mi[m], cows[k];
    for(int i; i<m, i++;){
        cin >> mi[i];
    }
    for(int i; i<k, i++;){
        int c, p;
        int cp[2] = {c,p};
        cin >> c >> p;
        cows[i] = cp;
    }

    
    
}